set ::wget::news(version) 2.1
set ::wget::news(message) {
		修正：代理伺服器設定值不會自動載入的錯誤。
		新功能：找出影音清單。
		新功能：抓太多圖被無名擋下時，會跳出視窗提示。
		新功能：可以調整下載項目的命令規則。
		新功能：可以調整抓取的間隔時間，避免抓太快。
		WGet的新家 : http://got7.org/blog/?page_id=7 
}
